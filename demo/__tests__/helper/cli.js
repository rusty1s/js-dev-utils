// const { getArgs, mergeArgs, run } = require('../../../helper/cli');

test('get arguments', () => {
  // Not really testable as the test arguments can vary :(
  // expect(getArgs()).toEqual(process.argv.slice(2));
});

test('merge arguments', () => {
  // const defaultArgs = ['--config', 'src', '--ignore', '-a', 'a', 'b', 'c'];
  // const args = ['--no-ignore', '-b', 'a', 'b'];
  // expect(mergeArgs(defaultArgs, args)).toEqual(["--config", "src", "--no-ignore", "-a", "-b", "a", "b"]);
});

test('run command', () => {});
